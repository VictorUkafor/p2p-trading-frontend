import axios from 'axios';

const state = {
    auth: false,
};

const getters = {
    getAuth: state => state.auth,
};

const mutations = {

};

const actions = {};


export default {
    state,
    getters,
    actions,
    mutations
}